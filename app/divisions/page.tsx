import Image from "next/image";
import Link from "next/link";
import { divisions } from "../brand-data";
import { DeferredVideo } from "../components/DeferredVideo";
import { MarketingShell } from "../components/MarketingShell";

export default function DivisionsPage(){return <MarketingShell><section className="parity-page-hero"><div className="parity-page-hero-image division-group"/><div className="parity-gridlines"/><div className="parity-page-hero-copy"><div className="parity-status"><i/> DIVISION NETWORK ONLINE</div><p className="parity-eyebrow">NINE SPECIALISTS. ONE CONNECTED SYSTEM.</p><h1>MEET THE<br/><em>DIVISIONS.</em></h1><p>The approved divisional P.O.L.A.R.s represent the nine specialized build lanes that execute what the Blueprint requires.</p></div></section><section className="parity-division-motion"><DeferredVideo src="/media/sites/polar.mp4" poster="/brand/divisions/polar/division-network-headquarters.webp" label="P.O.L.A.R. division network animation"/><div><p className="parity-eyebrow">P.O.L.A.R. // DIVISION INTELLIGENCE</p><h2>ONE NETWORK.<br/><em>NINE BUILD LANES.</em></h2><p>Each specialist keeps its own visual identity and capability lane while working from the same approved Blueprint.</p></div></section><section className="parity-division-grid">{divisions.map((division)=><Link className="parity-division-card" key={division.slug} href={`/divisions/${division.slug}`}><div className="parity-division-image"><Image src={division.image} alt={`${division.name} divisional P.O.L.A.R.`} fill sizes="(max-width: 640px) 45vw, (max-width: 1100px) 25vw, 17vw"/></div><div><span>{division.code}</span><h2>{division.name}</h2><p>{division.description}</p><small>ENTER DIVISION →</small></div></Link>)}</section></MarketingShell>}
