import Link from "next/link";
import { DeferredVideo } from "../components/DeferredVideo";
import { MarketingShell } from "../components/MarketingShell";
import { flagships } from "../brand-data";

export default function FlagshipsPage(){return <MarketingShell><section className="parity-page-hero"><div className="parity-page-hero-image flagship-network"/><DeferredVideo className="parity-hero-motion" src="/media/sites/bpei-launch.mp4" poster="/brand/approved/POLAR_INTERFACE_PROJECTION.png"/><div className="parity-hero-motion-shade"/><div className="parity-gridlines"/><div className="parity-page-hero-copy"><div className="parity-status"><i/> BUILD PATH ACTIVE</div><p className="parity-eyebrow">FIVE FLAGSHIP ENGAGEMENTS</p><h1>FROM RAW IDEA<br/><em>TO OPERATING REALITY.</em></h1><p>Each flagship solves a different stage of the same problem: turning founder intelligence into an enterprise that can function beyond the founder’s head.</p></div></section><section className="parity-journey-grid">{flagships.map((item)=><Link className="parity-journey-card" key={item.slug} href={`/flagships/${item.slug}`}><span>{item.step}</span><p>{item.eyebrow}</p><h2>{item.name}</h2><small>OPEN FLAGSHIP →</small></Link>)}</section><section className="parity-sequence"><span>REALISTIC ORDER</span><b>EXTRACTION → BLUEPRINT → BUILDOUT → FUNDING READINESS → DEPLOYMENT</b><p>Funding readiness is engineered throughout the process. The formal funding ask happens when the structure, story, numbers, and use-of-capital plan can survive scrutiny.</p></section></MarketingShell>}
